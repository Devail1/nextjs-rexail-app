import type {NextPage} from 'next'
import {TCategory, TProduct} from "../../types";
import {ReactNode} from "react";
import Head from 'next/head'
import StoreItem from "../components/storeItem";

import categories from "../../mockData.json"

const Store: NextPage = () => {
    let selectedCategory: TProduct[] = categories[0].children

    return (
        <div>
            <Head>
                <title>Create Next App</title>
                <meta name="description" content="Generated by create next app"/>
                <link rel="icon" href="/images/favicon.ico" />
            </Head>
            <nav id="categories" className="categories-wrapper">
                <ul className="container mx-auto h-full display-flex align-center justify-evenly font-gray-900">
                    {categories.slice(0, 10).map((category: TCategory): ReactNode => {
                        return <li key={category.id} className="category">
                            <button type="button">{category.name}&nbsp;</button>
                        </li>
                    })}
                </ul>
            </nav>
            <div className="container mx-auto">
                <div className="store-items-wrapper mt-30">
                    {selectedCategory.map((product: TProduct): ReactNode => {
                        return <StoreItem key={product.id} currencySign="₪" product={product}/>
                    })}
                </div>
            </div>
        </div>
    )
}

export default Store
